import styled from "styled-components";

export const Header = () => {
  return (
    <div>
      <nav>
        <button> Home </button>
      </nav>
      <nav>
        <button> Character </button>
      </nav>
    </div>
  );
};
